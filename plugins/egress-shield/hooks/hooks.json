{
  "description": "Hydra egress-shield: OPT-IN BLOCKING PreToolUse hook. Exits 2 when destination host is not in the allowlist; default-disabled.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "WebFetch|WebSearch|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/pretooluse.sh\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
